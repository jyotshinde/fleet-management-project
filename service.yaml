apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  labels:
    type: webapp-service 
spec:
  selector:
    type: my-webapp
    version: "0-5"
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
